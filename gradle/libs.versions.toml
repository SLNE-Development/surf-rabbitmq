[versions]
amqp-client = "5.30.0" # Update netty version if needed
netty = "4.2.14.Final" # Keep in sync with https://github.com/rabbitmq/rabbitmq-java-client/blob/main/pom.xml

[libraries]
amqp-client = { module = "com.rabbitmq:amqp-client", version.ref = "amqp-client" }
netty-bom = { group = "io.netty", name = "netty-bom", version.ref = "netty" }