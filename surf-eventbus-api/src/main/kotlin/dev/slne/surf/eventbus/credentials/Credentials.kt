package dev.slne.surf.eventbus.credentials

/**
 * What a [dev.slne.surf.eventbus.credentials.provider.CredentialsProvider] hands back: the
 * address and secret a broker connection is opened with, and nothing about how.
 */
interface Credentials
